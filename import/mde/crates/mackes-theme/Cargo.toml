[package]
name        = "mackes-theme"
description = "Mackes Desktop Environment (MDE) — Carbon → cosmic-theme adapter that parses the canonical CSS-token file into typed Rust values."
version.workspace      = true
edition.workspace      = true
rust-version.workspace = true
license.workspace      = true
authors.workspace      = true
repository.workspace   = true

[lib]
name = "mackes_theme"
path = "src/lib.rs"

[dependencies]

[lints]
workspace = true
