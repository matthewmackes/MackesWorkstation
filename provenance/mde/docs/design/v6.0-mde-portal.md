# v6.0 — MDE Portal

**Status:** Locked 2026-05-24/25 across 10 question-survey rounds (~573 locked decisions).
**Worklist:** `docs/PROJECT_WORKLIST.md` § v6.0 mde-portal — Portal-* / VOIP-* / MESH-* / CONTAINER-* prefixes.
**Memory:** `~/.claude/projects/-home-mm-Desktop-files-mackes-shell/memory/project_v6_0_mde_portal.md` (extended rationale + open follow-ups).

---

## 0. Goal + brand statements

**Goal (locked 2026-05-25):**
*"Create a world-class and well-unified Portal and control plane that is highly pleasing to work with. Riding alongside i3."*

**Brand statements:**

- *"Two primitives. Every object is a card. Every navigation is a breadcrumb. The platform is built from nothing else."* (R5-Q25)
- *"The breadcrumb is the platform: every action, every notification, every navigation step lives in one continuous line of meaning that grows and breathes with you."* (R4-Q100)
- *"The mesh you cannot see is the mesh you cannot trust. Every peer, every host, every flow — visible by default, secret only by choice."* (R8-Q100)

---

## 1. Mental model

### 1.1 Cards-and-breadcrumbs axiom (R5)

Every object in MDE Portal is a **card**. Every navigation step is a **breadcrumb segment**. These are the only two primitive UI surfaces. Cards and segments are the same primitive in different render modes.

**Card schema (12 fields, R5-Q3):**

```json
{
  "id": "<stable-mesh-merged>",
  "type": "app|file|peer|activity|setting|workspace|tag|tray|zone|contact|container|...",
  "name": "...",
  "icon-uri": "...",
  "color-tag": "...",
  "parent-tag(s)": ["..."],
  "state": { "read": false, "starred": false, "pinned": false, "tags": [...] },
  "actions": [{"label": "...", "primary": true, "...": "..."}],
  "description": "...",
  "source-peer": "<peer-uid>",
  "ts-modified": "<iso8601>",
  "body-text": "...",
  "children": [/* recursive Card[] for composite cards */],
  "schema_version": 1
}
```

**Card identity (R5-Q9):** Stable IDs are derived from the underlying object — apps = `.desktop-id`, files = `<peer>:<path>`, peers = mesh-uid, activities = ULID. Cross-peer view auto-merges identical cards (Firefox-on-laptop and Firefox-on-phone unify).

**Card render modes (6, R5-Q2 + R5-Q7 + R5-Q17 + R5-Q21):**

1. **segment** — compact, mono, chevron-bordered (breadcrumb-segment context)
2. **cascade-card** — full icon + name + 1-line description + Actions button (Hub default)
3. **list-row** — dense single-line (Library long lists, notifications popover)
4. **mini-tree-cell** — chevron-bordered cell with number + name + thumbnail (workspaces, tray icons)
5. **lock-widget** — special lock-screen rendering for clock/weather/battery/etc.
6. **hero** — full-bleed 3x-cascade-card-height poster (enriched-card default; tall poster with artwork + title + Wikipedia snippet + 'Read more' chip)

**Cross-mode animation:** shared-element morph + spring physics overshoot (~250ms). Cards feel like persistent objects, not redraws (R5-Q8).

**Card composition (R5-Q10):** Cards have an optional `children: Card[]` field. Tree-of-cards is the canonical data structure. Cascade columns render `parent → child` drills.

### 1.2 Three-scale Portal model (R3-Q50)

The user-facing surface is ONE THING at three scales:

| Scale | Trigger | Form |
|---|---|---|
| **Dock** | Always-visible | 56px bottom strip; horizontal breadcrumb |
| **Portal-compact** | Mod+Space (or Mod+\\ on IBus systems, R6-Q7) | 336px-tall floating strip; mesh-glance globe |
| **Portal-full** | Super-tap | Full-screen scratchpad shell; 5 layers |

Dock↔Portal-full transition is an **element-by-element morph** (R3-Q49): each Dock element animates to its corresponding position in Portal; the Dock IS Portal in collapsed form.

### 1.3 Wayland surfaces (6 total)

mde-portal owns six Wayland surfaces in a single Rust process:

1. **Dock** (wlr-layer-shell `bottom`, exclusive). 56px tall, per-output (R3-Q1, R3-Q45).
2. **Portal-compact** (wlr-layer-shell `top`, summoned via Mod+Space/Mod+\\). 336px tall.
3. **Portal-full scratchpad** (wlr-layer-shell `top`, summoned via Super-tap; hidden via i3 scratchpad).
4. **Lock screen** (wlr-layer-shell `overlay`, replaces swaylock theming).
5. **Boot/shutdown theater** — full-screen overlay rendering real-time systemd telemetry around an assembling/dimming M watermark (2–3s budget each).
6. **Mesh-wallpaper** (wlr-layer-shell `background`, R6-Q27). 12s default update (6–60s range), auto-pause when fully obscured.

App-switcher **lives on the Dock** (R3-Q41 reverses R2-Q55). Alt+Tab cycles running-app icons with Aero-peek above current. No separate switcher surface.

---

## 2. Dock — the canonical breadcrumb

### 2.1 Layout (R3-Q36, R4-Q1 + R10-Q44)

Reads as a horizontal **breadcrumb** left-to-right, with subtle 16px chevrons (R3-Q37) and 15px Intel One Mono Bold for the active rightmost segment (R4-Q10, R4-Q27):

```
[Apps › Files › Notifications › VoIP › Network › Settings] › [mini-tree(+)] › [hostname[role]] › [pinned-zone] › [running-zone] › [status-zone] › [tray] › [clock/date] › [show-wallpaper-strip] › [view-tail extension]
```

**Bidirectional growth (R4-Q34, R8-Q34):**
- **Notification segments** appear on the LEFT (left of buttons), reading as "incoming events"
- **View-tail extensions** append on the RIGHT (after show-wallpaper), reading as "navigation depth"

### 2.2 Direct nav buttons (R10-Q1 to R10-Q15)

The M-badge from R3-Q2 is **retired** (Q113 directive + R10-Q6). M moves entirely to the wallpaper watermark. 6 direct buttons replace it:

| Button | Carbon glyph | Click target | Tier-pulse color (R10-Q3) |
|---|---|---|---|
| Apps | `grid` | Portal-full Hub | indigo |
| Files | `folder` | Portal-full Library | sage |
| Notifications | `bell` | Library w/ notifs-filter saved view | amber-on-pulse, neutral otherwise |
| VoIP | `phone` | Portal-full VoIP layer | purple |
| Network | `globe` | Portal-full Network layer | cyan |
| Settings | `settings` | Portal-full Control | charcoal |

Each button:
- 36px (R10-Q2), same size as Dock app icons, with 16px chevron between
- Carbon glyph monochrome (Carbon first, Nerd Glyphs fallback per directive)
- Tier-based pulse + numeric count badge top-right (R10-Q3)
- Per-button-specific right-click menu (R10-Q5)
- Tonal-inversion active-layer indicator (R10-Q15)
- Domain-color chevron to the left of each (R10-Q46)

### 2.3 Mini-tree (R4 directive + R10-Q44)

Position: immediately after the 6 buttons, before hostname. Cells use **chevron-as-border internally** (R4-Q63) so the chevron motif propagates inside the tree. Cells: number badge + name in Plex Mono (now Intel One Mono per directive) + thumbnail (R4-Q66).

Adaptive width with 24px floor + marquee on overflow (R4-Q64). All workspaces across all outputs visible; current-output highlighted (R3-Q46). `+` button at right end spawns new workspace (R3-Q24). Hover Aero-peek for full workspace screenshot (R3-Q23). Smooth 200ms morph + 300ms change-pulse on updates (R4-Q65).

### 2.4 Hostname segment (R3-Q38, R4-Q6, R4-Q61)

Format: `<hostname>:<output-name> [leader]` when this peer holds the QNM-Shared lock. Click cycles through OTHER peers' Portal states (cross-peer view — see §3.4). Hover tooltip: peer status card (uptime / IP / mesh role).

**Pre-mesh-home state:** Hostname shows `<hostname> (local-only)` muted + offline-mesh glyph (R4-Q46).

### 2.5 Pinned + running zones (R3-Q9 to R3-Q18)

**Pinned zone** (drives by designated taskbar-tag, Q88):
- 36px icons + open-window-count pip; click expands inline Win10-style tile listing windows (R3-Q9, R3-Q16)
- Pin via drag-from-Hub OR right-click; unpin via drag-off-Dock OR right-click (R3-Q12)
- 350ms-hover live Aero-peek (R3-Q11)
- Middle-click = launch new instance (R3-Q17); scroll-wheel cycles app windows (R3-Q18)
- Chevron-popover overflow at 36px floor (R3-Q14)

**Running zone** (R3-Q15, R3-Q16):
- All workspaces, corner WS-badge
- Click: 0 → launch, 1 → focus (jumping to ws), 2+ → expand Win10-tile list
- Right-click menu mirrors Hub card menu + Unpin + Close-all (R3-Q10)

**WM buttons on hover** (R4-Q67 to R4-Q71):
Hovering any running-zone icon reveals 5 micro-buttons (16px each) below: `× close / float / full / – minimize-to-scratchpad / layout-cycle`. Glass-blur backdrop, 100ms fade-in. Layout button cycles split-h → split-v → tabbed → stacked on click; right-click for direct picker (R4-Q69).

**File pins (R3-Q42, R3-Q43, R8-Q43):** Drop a file from Library onto Dock adds the designated taskbar-tag to file's xattr; file appears in pinned zone as same-shape card (file-thumbnail in icon slot per R2-Q24).

### 2.6 Status zone (R3-Q32 to R3-Q35, R4-Q56, R4-Q74)

Consolidated single segment (no chevrons between widgets, R4-Q56). Contains:

- **Battery glyph + percent** (amber <20, red <10); click → power-profile popover (R3-Q32)
- **Combined network+mesh glyph** + peer-count pip; click → network/mesh popover with current SSID/IP + mesh peer list + tunnel diag (R3-Q33)
- **Speaker glyph** + scroll-volume + click → mixer popover (R3-Q34)
- **Sun glyph** (when backlight detected) + scroll cycles 5 discrete brightness levels 20/40/60/80/100% + click reveals 5-button picker (R3-Q35)
- **Lock glyph + Power glyph** — moved here from R3-Q92 top-right-of-Hub position (R4-Q74). Lock = instant; Power = popover with Sleep/Logout/Reboot/Shutdown.

**Single unified strip** slides up from Dock for any widget click (R4-Q76, R4-Q77); horizontal slide-swap content transition between widgets (R4-Q78). Strip canvas = grid-of-cards (R4-Q79), ~25% screen height, full-width.

### 2.7 Tray (R3-Q25 to R3-Q30)

StatusNotifierItem (R2-Q65). 24px right-aligned, first-seen order (R3-Q25). Each tray icon is a card in mini-tree-cell mode (R5-Q12). Click activates (SNI Activate); right-click menu (R3-Q26). Tier-pulse vocabulary shared with M-badge for NeedsAttention (R3-Q27). 5-icon cap + chevron overflow popover (R3-Q28). **Full XEmbed support** for legacy apps (R3-Q29). Tooltip = app name + SNI ToolTip text + Aero-peek (R3-Q30).

### 2.8 Clock + show-wallpaper (R4-Q55, R4-Q72, R4-Q73)

**Clock** — far-right segment after tray (R4-Q55). 24h time + small date below. Click opens calendar popover.

**Show wallpaper strip** — far-right edge of Dock (R4-Q72, R4-Q73). Thin slim clickable strip with landscape glyph. Click toggles: first click → all visible windows slide to scratchpad with synchronized animation; second click → restore. 3-finger swipe down also triggers (R4-Q81).

### 2.9 Notifications segment cluster (R4-Q34, R4-Q35, R10-Q7)

With M-badge gone, notification segments **localize around the Notifications button** as a hover popover (R10-Q7). Vertical stack of 3 cards (list-row mode), tier-color + glyph + source + 1-line body; click row → smart-default action; 'x' dismisses; 'View all' chip → Library-with-notifs-filter (R10-Q8). Crit-tier additionally fires a transient toast top-center of focused output (R10-Q9).

Lifetime tier-based: crit forever, warn 60s, info 8s (R4-Q40). Stack-collapse with count badge (Slack×3 counts as 1 toward 5-cap; R4-Q42). Cross-peer notifications merged with peer-badges (R4-Q44, R8-Q44).

### 2.10 Breadcrumb behavior

- **Typewriter character-by-character reveal** on segment append, 60 chars/sec (R4-Q22, R4-Q23). Reverse-typewriter on collapse (R4-Q24).
- **Marquee scroll** on overflow at 50 px/sec, pause-on-hover, click-jump (R4-Q60, R4-Q21).
- **Subtle typewriter tick sound only** (R4-Q28).
- **Chevron-color rule:** every chevron takes the color of its right-side segment, regardless of context (R4-Q25, R10-Q45).
- **Segment text** in its own group color (R4-Q26).
- **Active rightmost segment:** bold + 17px (R4-Q27).
- **Solid charcoal Dock strip** (off-white in light mode), supersedes R3-Q29's translucent for the bottom baseline (R4-Q75).
- **Continuous breath-line** under breadcrumb: subtle gradient sweep blending current-state colors, 15s cycle (R4-Q90, R4-Q91).
- **Click on intermediate view-tail segment** jumps to that drill level with reverse-typewriter collapse (R4-Q30).
- **Drag segment OUT of breadcrumb** drops as `mde://` URI on any XDND receiver (R4-Q17).
- **Hover popover** = type-specific cascade-card render of the underlying card (R4-Q18, R5-Q20); if enriched, hero render. Shift-hover sticks the popover; Ctrl-click permanently pins (R4-Q92).
- **Tap-and-hold breadcrumb** expands compact-mode into full-mode 3s peek (R4-Q95).

### 2.11 Live state in breadcrumb

- **New mesh peer comes online (R4-Q31):** three-way live state — hostname segment glimmers + temporary "+ peer" chip for 5s + mini-tree highlight.
- **Window focus change (R4-Q52):** very fast scroll-pulse, then normal.
- **New SNI source registers (R4-Q53):** "welcome" notification segment 'Slack ready' fires as info-tier; standard dismiss.
- **Workspace switch (R4-Q51):** mini-tree highlight shifts; rest unchanged.
- **Network change (R4-Q76):** comet-trail relocation animation of peer dot + info-tier 'Network changed' notification.
- **Public IP rotation (R8-Q79):** info-tier notification on every rotation.

### 2.12 Edge cases

- **Fullscreen** (R4-Q32 supersedes R3-Q39): 22px full-width mini-Dock with M + breadcrumb + volume + brightness only.
- **Lock screen** (R4-Q4): minimal `M › hostname` only.
- **Compact mode** on portrait / narrow (R4-Q93, R4-Q94): drop priority running-zone first, keep clock + M + hostname + view-tail.
- **State persistence** (R4-Q48): full restore on process restart (drilled path + notification segments + cross-peer view if active).
- **No RTL in v6.0** (R4-Q84) — English-only.
- **No hide-breadcrumb** option (R4-Q86); Mod+F11 for temporary auto-hide only.

---

## 3. Portal-full scratchpad

Five layers, each reachable via direct-nav button click OR Super+digit when scratchpad has focus (R2-Q109). Layer state fully persists across switches (R2-Q111). Each layer deep-links via `mde://<layer>/<path>` URI (R2-Q51).

### 3.1 Hub layer (Apps)

Hierarchical cascade launcher.

- **System tags** at cascade root: All apps / Untagged / Workspaces / Settings / Power / Mesh. ('Recent' system tag retired per R3-Q20.)
- **Tags universal across apps + files + activities + peers** (R2-Q22, R2-Q23 xattr `user.mde.tags`)
- **3-level nesting cap** (R5-Q5 matches Hub's Q52)
- **User-curated from scratch** (Q48)
- **Three tag flavors:** manual (default), smart (⚡, predicate-driven), preset (⚗, launch-bundle, Q63)
- **One designated tag drives the Dock pinned-zone** (Q88, set in Edit-tags modal)
- **Tag config mesh-synced** via GlusterFS (Q56)
- **Card reorder happens ONLY in Edit-tags modal** (R1-Q103) — cascade is pure navigate
- **Tag intersection via sticky multi-select** on tag cards (R2-Q77, R2-Q78 full color fill + checkmark badge)
- **Tag-driven workspace policy** (Q59 + Q60): cascade-context tag wins, search falls back to root-order priority
- **Per-tag layout default** (Q61): tag carries split/tabbed/stacked/floating; apps inherit
- **Right-click card** = iconic Layout Chooser (split-h/v/tabbed/stacked/floating/fullscreen/new-workspace; user directive Q62) + Hub right-click standard menu

### 3.2 Library layer (Files / Activity / Notifications)

File manager + activity browser. Replaces mde-files (hard cut, R1-Q108).

- **Landing: XDG cascade-card grid** (R5-Q4): Documents / Downloads / Pictures / Videos / Music / Activity / Other
- **Sidebar 2-section** (R2-Q17): Mesh peers + Views; XDG dirs nested under peers
- **Tabs (Ctrl+T/W/Tab)** within Library (R2-Q30)
- **3-level depth cap** smart-collapse (R5-Q5)
- **File-op feedback** as progress cards in left-of-Notifications-button hover popover area + activity store (R5-Q6)
- **File selection** = dual presence: cascade-card selected + breadcrumb tail extends (R5-Q11)
- **Activity files** browseable under `activity/` like any other folder; no special 'feed' surface (R3-Q20)
- **Mesh-aware trash** at `~/.local/share/mde/trash/`, 5 GB cap, oldest-first eviction (R2-Q28, R2-Q29)
- **Drag + right-click file operations** (R2-Q27); XDG Trash spec extended for mesh
- **xdg-desktop-portal-mde** opens Library in "picker mode" (R2-Q67) for app file pickers
- **Notifications button → Library with notifs-filter saved view** (R10-Q4)
- **Library default landing** TBD post-R3-Q20: XDG cascade grid; activity folder accessible like any folder (R5-Q4)

### 3.3 Control layer (Settings)

Same cascade UX as Hub (R2-Q41). 6 meta-categories (R2-Q42 + R10-Q16):

1. **Hardware** — display, audio (w/ Bluetooth inline), keyboard, storage
2. **Network** — Mesh (comprehensive), wifi, wired, VPN
3. **Customize** — themes (3-lever: 12-pastel accent + dark/light + font scale), input
4. **Maintenance** — updates, power
5. **Containers** — Podman full management (§9)
6. **About** — version + Cairo brand banner

**Mesh sub-panel** (R2-Q43): status + peers + passcode rotation + GlusterFS health + Nebula tunnel diag (single pane).

**Updates panel** (R2-Q45): pending count + 'Update now' button only.

**Display panel** (R2-Q46): resolution / scale / rotation / arrangement / detect / night-light / color-profile / refresh-rate / VRR.

**Audio panel** (R2-Q47): PipeWire sink/source picker + per-app mixer + mic test + KDC2 routing + Bluetooth inline (no separate Bluetooth panel).

**Keyboard panel** (R2-Q48): layout + repeat + custom shortcuts editor (override any mde-portal binding) + global keybindings list.

**Storage panel** (R2-Q50): per-volume bar + mesh-home replication bar + Manage trash / Clean caches.

**About panel** (R2-Q49): version + hostname + Linux + mesh peer count + 'Check for updates' / 'Release notes' / 'Issue tracker' actions + Cairo brand banner with hashbang nod.

### 3.4 VoIP layer

Default landing: dialpad + recent calls only — Mac Phone-app minimal landing (R10-Q10). View-tail: `› VoIP:<state>` (R10-Q11).

**SIP architecture (Round 9 + R11 lock 2026-05-26 — Kamailio retired):**

R11 supersedes R9-Q1/Q2/Q4. The Kamailio-per-peer container model is removed
entirely; PJSIP registers directly to a per-peer Vitelity sub-account using
its native multi-account support. Each peer runs **two PJSIP accounts
simultaneously**: a mesh-internal extension and a Vitelity sub-account.

**Birthright deploys zero containers** (R11 clarification 2026-05-26). The
VoIP stack runs as native processes inside `mded`/`mde-portal`. Podman
remains in the architecture exclusively for the user-managed CONTAINER-*
feature surface in Control (operator pulls/runs/manages their own pods +
containers). The **only** container the VoIP stack itself may spawn is an
on-demand `mediasoup` media bridge during 3+ party calls (see VOIP-9) —
spawned by `mded` at call-start, torn down at call-end, never at install
or login.

- **PJSIP as the only SIP stack** — in-mde-portal, no SIP server in the loop (R8-Q86 + R11 lock)
- **Two accounts per peer** (R11): account 1 = `sip:<user>@mesh.mde` for intra-mesh; account 2 = `sip:<sub-account>@sip.vitelity.net` for PSTN. Both register on `mded` startup.
- **Vitelity sub-account creds** stored per-peer in `~/.config/mde/voip/vitelity.toml`, provisioned by Birthright (R11; supersedes R9-Q2 container-secrets model)
- **Bare-hostname dialing** resolves via mesh DNS (`<host>.mesh.mde`, R9-Q3) — intra-mesh INVITE goes peer→peer directly, no relay
- **External calls** route through the dialing peer's own Vitelity link (R11; supersedes R9-Q4 smart-routing). Optional: `mded` broadcasts Vitelity-link RTT via Bus; dialer UI shows the local link's RTT and can suggest "Place via <peer>?" before dialing if a markedly better link exists. No automatic re-route — operator-explicit only.
- **Inbound:** Vitelity-side SimRing across all peers' sub-accounts; each peer's PJSIP reads focus state from Bus and responds **`486 Busy Here`** immediately if `focused_peer != self`. Only the focused peer answers (R11; supersedes R9-Q5 Kamailio fork)
- **Tiebreaker:** when two peers both claim focus, the lower mesh-ULID wins (Bus ordering); the other returns 486
- **Voicemail:** Vitelity voicemail-to-email → mesh-home `~/Documents/Voicemail/<peer>/<iso8601>.mp3` + transcript card (R9-Q6, unchanged)
- **Video:** VP9 default + H.264 fallback + AV1 opt-in + SRTP + adaptive 90p–1080p (R9-Q7, unchanged)
- **Call UI:** Portal-full overlay (R9-Q8, unchanged)
- **Group calls:** PJSIP can't mix N participants, so calls >2 spawn an **ephemeral media bridge** (mediasoup) on the best-uplink peer for the duration of the call; signaling coordinated via Bus, bridge container torn down on last-leave (R11; supersedes R9-Q9 Kamailio focus). 8-participant cap preserved.
- **Recording:** per-peer toggle in Customize, off by default (R9-Q10, unchanged)
- **SMS:** Vitelity API → cards in calls/ activity, inline replies, mesh-synced threads (R9-Q11, unchanged)
- **Contacts:** universal cards (type='contact') in mesh-home (R9-Q12, unchanged) — fully participate in tag system
- **Audio routing:** PipeWire role-based + Mod+Space PTT during calls (context-sensitive, R9-Q13, unchanged)
- **Echo cancellation + noise suppression:** RNNoise + WebRTC AEC3 (R9-Q14, unchanged)
- **Adaptive bitrate cascade** with 'connection adapting' chip (R9-Q15, unchanged)
- **5 ringtones + per-contact override** (R9-Q16, unchanged)
- **Encryption:** Nebula intra-mesh + SRTP-AES external (R9-Q17, unchanged)
- **Multi-mesh federation:** SIP-over-Matrix bridges (R9-Q18) — **deferred past 1.0** under R11; without Kamailio there's no per-peer SIP server to host the bridge. Reinstating means either a single-gateway-peer asymmetry (one peer runs `matrix-appservice-voip`) or a different federation mechanism. Open question.
- **NAT traversal:** Nebula UDP-hole-punching intra-mesh; PJSIP SIP UDP/TLS with rport + STUN for external Vitelity registration (R9-Q19, unchanged)

**Active call edge viz** on globe (R8-Q85): living purple edge + phone-glyph midpoint + audio-sync pulse + 1Hz throb; same for ANY SIP client activity.

### 3.5 Network layer (Portal-full mesh view)

Distinct from Portal-compact (Mod+Space mesh-glance). Same data, deeper analysis. Full-screen globe at 4x larger + side-panel peer-cards list + detail pane (R10-Q13).

See §7 for the full mesh-view subsystem shared between Portal-compact + Portal-full Network + Mesh-wallpaper.

---

## 4. Portal-compact (Mod+Space)

The cross-peer mesh-glance surface (R6-Q1).

**Visual style (R6-Q4, R8-Q42, R8-Q45):**

- Dark-mode rotating globe (2D-flat projection, simulated rotation via horizontal pan)
- Sparse: 1px coastline outlines, empty oceans (background), data is the heat
- Auto-rotate 30s/full + drag + scroll-zoom (3 zoom levels: world/continent/city)
- 336px-tall full-width strip (R6-Q6)
- Continents render single-color (charcoal in dark / off-white in light)
- Color-coded Light/Dark (auto-adaptive per R2-Q44)

**Peer geo (R6-Q3):** Auto IP-geo + user override; MaxMind GeoLite2 bundled offline (R8-Q77).

**Interactions (R6-Q5, R8-Q71):**

- Click peer dot → cross-peer cycle to that peer (R4-Q6)
- Right-click peer dot → 'Zoom into LAN' (R8-Q30)
- Scroll-wheel = zoom (3 levels); drag = manual rotate
- **No touchpad gestures** (R8-Q71) — mouse/keyboard only

**3-mode toggle** (R8-Q3): Globe / Wireframe / Hybrid; corner icon cycles.

**Heatmap overlay** (R8-Q97) toggleable on top of any view mode.

**Multi-monitor:** each output independent (R4-Q29).

---

## 5. Mesh-wallpaper (R6-Q27, R8-Q42)

6th Wayland surface (wlr-layer-shell `background`). Same rendering pipeline as Portal-compact at lower fidelity. **12s default update** (6–60s range, configurable). Auto-pause when fully obscured. View-only — interactivity only via Portal-compact (R6-Q28).

**System load:** ~0.3–0.8% CPU avg, ~50–80MB RAM when visible; ~0% CPU when paused.

---

## 6. Cards subsystem

### 6.1 Universal model

See §1.1 for schema + render modes + identity + composition.

### 6.2 Enrichment subsystem (R5-Q14 to R5-Q19, R10-Q36 to R10-Q43)

Cards augmented with external metadata via 9+ sources:

- Wikipedia (text + thumbnail)
- MusicBrainz / CoverArtArchive (album art)
- TMDB (movie/TV posters)
- GitHub (README + avatars for repo files)
- Open Food Facts (product images)
- OSM Nominatim (geo-tagged files)
- PyPI / crates.io (code repos)
- Recipe sites
- Local thumbnailer (freedesktop XDG thumbnail spec at `~/.cache/thumbnails/{normal,large,x-large}/` via tumbler-style daemon — R5-Q22)

**Behavior:**

- Lazy fetch on card first-view (R5-Q16); cache forever until manual re-enrich (R10-Q40)
- Storage inline in card JSON (R5-Q15)
- Direct per-peer fetches; per-source toggle in Control; default ALL ON (R5-Q19)
- Multi-source merge: TMDB image + Wikipedia summary both shown with cite chips (R10-Q42)
- Soft caps: 100MB cumulative cache per card; 10MB/hr per peer; pause at mesh-home >90% (R10-Q43)
- Enriched cards render in **hero mode** by default (R5-Q17): 3x cascade-card height poster + artwork + Wikipedia snippet + 'Read more' chip (R5-Q18)
- **Text-only hero fallback** (R10-Q38): full-bleed accent-color background + oversized Intel One Mono Bold title
- **Failure UX** (R10-Q39): silent retries with exponential backoff; no UX surface
- **Per-tag opt-out** via 'no-enrich' tag (R10-Q41)
- **Schema versioning** with migration registry (R10-Q36); forward-compatible across mesh peers (R10-Q37)

---

## 7. Mesh-view subsystem (R7 + R8)

Shared rendering between Portal-compact, Portal-full Network layer, and Mesh-wallpaper.

### 7.1 Per-peer network assessment (R7-Q1 to R7-Q7)

Each peer collects 9 items hourly + on Portal-compact open (R7-Q2):

1. WiFi SSIDs + RSSI + channel + encryption
2. Local ARP table
3. Default gateway + DNS servers
4. Public IP + ISP/AS
5. Speedtest (down/up/latency)
6. IPv4 + IPv6 connectivity
7. MTU + jumbo-frame support
8. Tunnel health
9. nmap-light passive subnet discovery

Storage: `~/.local/share/mde/netassess/<peer-hostname>/<iso8601>-<hash>.json` mesh-synced, 30-day rolling (R7-Q3). Backend: Rust + shell-out to mature tools (R7-Q4). Data enriches all platform surfaces.

**Route traces** (R7-Q7) to: lighthouse + all peers + gateway + 1.1.1.1 + 8.8.8.8 + GlusterFS leader.

### 7.2 Crowdsec XDR (R7-Q5, R7-Q6, R8-Q18, R8-Q23)

Crowdsec assimilated as XDR backbone. Alerts→activity files; real-time CTI enrichment on globe (red-outline flagged externals + threat-category chip, R8-Q81); behavioral anomaly detection for flow surges (R8-Q18); auto-ban on probe (R8-Q23); auto-isolate on mass-attack (R8-Q96).

### 7.3 Surrounding hosts (R8-Q1 to R8-Q15)

Surrounding LAN-neighbor devices that aren't mesh peers. Rendered on globe with hollow-outline dots + type-glyph, orbiting their discovering peer (R8-Q1, R8-Q2).

**Identification (R8-Q8):** mDNS + DHCP vendor-class + MAC OUI + reverse-DNS PTR + HTTP banner + active TCP/IP fingerprint.

**14 host types (R8-Q9):** Router / Switch / AP / Printer / NAS / Camera / TV-Cast / Smart-speaker / IoT / Phone / Computer / Server / Game-console / Unknown.

**Trust states (R8-Q10):** trusted / unknown / blocked.

**Card actions (R8-Q11):** click expands to cascade-card preview (IP/MAC/vendor/services); right-click: Trust / Block / Ping / Browser / Add-to-mesh.

**Discovery (R8-Q12):** Hybrid: passive listening always-on + active 10min sweep + manual refresh.

**Mesh-shared (R8-Q13):** All peers see all LAN-neighbors.

**Duplicate handling (R8-Q14):** Single card with multi-sighting badge + roaming history.

**Stale expiry (R8-Q15):** 7-day fade + 30-day archive.

**Block enforcement (R8-Q44):** Mesh-coordinated iptables DROP via mesh-home consensus.

**MDE peer auto-detection (R8-Q90):** Info-tier suggest-pair notification + consent.

**Onboarding wizard (R8-Q35):** Inline wizard with probe (`mde://discover/`) → pair-with-passcode or install-first script + QR.

**Port-based connect (R8-Q50):** 12 well-known port mappings — SSH/HTTP/HTTPS/VNC/RDP/SMB/FTP/CUPS/psql/mysql/redis/HTTP-alt/mongosh.

### 7.4 Wireframe / EtherApe mode (R8-Q3 to R8-Q6)

Force-directed graph (R8-Q4). Edges encode (R8-Q5):

- **Thickness** = current bandwidth (log scale)
- **Color** = protocol (HTTPS green, SSH blue, DNS yellow, custom pastel)
- **Animation** = arrow particles flowing at packet rate

**Per-protocol line styles (R8-Q46):**

- Nebula = dashed gold
- GlusterFS = solid blue (cyan particles for file-sync per R8-Q88)
- KDC2 = dotted purple (purple-throb + audio-sync for active calls per R8-Q85)
- Activity-sync = thin grey
- MDE-internal traffic = thin grey edges, distinct from third-party (R8-Q61)

**Every edge labels its transport encryption** (R8-Q19).

**Filter (R8-Q16):** protocol toggles only, no time-window (live last 1m).

**Flow collection (R8-Q6):** pure Rust eBPF module loaded by mde-portal.

**Flow privacy (R8-Q7):** all raw — consistent with open-mesh flat-trust.

**Storage growth (R8-Q48):** pyramid downsampling (1s→1m→1h→1d, 90d cap).

### 7.5 External destinations (R8-Q17, R8-Q47, R8-Q89)

ASN-clustered cloud-glyph nodes at AS-HQ geo (R8-Q17). Hover shows AS number + name + owner country + bytes-today + Wikipedia enrichment + Crowdsec CTI threat-intel (R8-Q47).

**BGP/AS-path visualization (R8-Q89):** hop-dots AS-grouped; AS-transition labels.

### 7.6 Globe-specific peer viz

- **Lighthouse:** pyramid/beam shape (R8-Q70); smooth failover animation + crit notification (R8-Q82)
- **Leader peer:** glowing crown glyph above dot (R8-Q34)
- **Subnet halos:** auto-hashed /24 prefix + user override (R8-Q68)
- **Day/night terminator overlay** + moon-icon on night peers (R8-Q65)
- **Heat-glow halo** for power draw (R8-Q67): charcoal/blue/amber/red
- **Inner WiFi signal-strength glow** + channel-busy arc (R8-Q58)
- **Connection-type glyph inside dot:** 🌐 Ethernet / 📶 WiFi / 📱 Cellular / 🔒 VPN-only (R8-Q59)
- **30px latency sparkline** below each peer dot (R8-Q69)
- **Speedtest color tier:** green >50/amber 10–50/red <10 Mbps (R8-Q32)
- **Bandwidth quota ring** when set (R8-Q33)
- **Surge inflation + ripple + flame glyph** on traffic spikes (R8-Q55)

**Latency (R8-Q56):** dual encoding — color-grade + wiggle.

**Multi-path edges:** stacked text label `DERP 23ms / LAN 8ms (active)` (R8-Q57).

**Hops as AS-grouped dots** with hover-detail (R8-Q20).

### 7.7 Network defense surfaces

- **ARP-spoof:** detect + auto-static-ARP via Crowdsec-style decision (R8-Q53)
- **Rogue DHCP:** detect only, manual action (R8-Q54)
- **Evil-twin AP:** BSSID + signal-fingerprint baseline; warn-before-connect (R8-Q60)
- **Captive portal:** canary-URL probe; crit-tier notification + one-click 'Open in browser' (R8-Q31)
- **DNS leak:** detect VPN-recommended-resolver mismatch + one-click fix (R8-Q41)
- **TLS observation:** eBPF SNI + Certificate capture + MITM detection (R8-Q78)
- **Lateral movement:** Crowdsec alert only (R8-Q75)
- **Persistent attacks:** single accumulated alert with 24h auto-ack if attack stops (R8-Q74)
- **Multi-peer surge:** unified inbox with peer-badged segments (R8-Q99)
- **Cross-peer notifications:** both peers' merged with peer-badges (R8-Q44)
- **Mesh-wide DND** with greyed segments (R4-Q39 softens R2-Q38)
- **Per-source mute** right-click action (R2-Q40)

### 7.8 Multicast + workspace viz

- **Multicast/broadcast:** expanding-ring waves from source (R8-Q64)
- **Multicast groups:** always-visible orbital rings (R8-Q91)
- **DHCP server:** confirmed-only label after DHCPACK observation (R8-Q92)
- **WiFi channels:** on-demand right-click 'Show channel scan' modal (R8-Q93)
- **Split-brain detect:** crit alert + disjoint mesh-color halos + recovery prompt (R8-Q94)
- **Mesh-DNS:** opt-in toggle in Control (R8-Q83) — leader peer runs Unbound/dns-rs
- **DNS-sniffing browser titles** (R8-Q49) — passive correlation

### 7.9 Cross-peer remote control

Per R4-Q6 + R4-Q7 + R8-Q63: hostname-cycle to a peer → **full RW** remote control via SSH/KDC2:

- Visual: **4px amber border + 'Viewing: <peer>' corner badge** (R4-Q8)
- Listening services + top-processes panel (R8-Q62, R8-Q63)
- Right-click process: Kill / Restart / View-logs in Library
- **Always-confirm + recipient audit notification** (R8-Q98)
- M-click / hostname-cycle / Esc-twice return (R4-Q9)
- **Optimistic-with-rollback** action feedback (R8-Q98); red on 10s failure
- **Representation only** via mesh-synced `~/.local/share/mde/state/breadcrumb.json` (R8-Q97)
- TZ display switches to remote-peer's TZ during cross-peer view (R8-Q95)
- M-badge **360° spin** on cross-peer return (R4-Q96)

---

## 8. VPN integration (R8-Q36 to R8-Q41)

**5 providers:** Mullvad / ProtonVPN / IVPN + WireGuard-generic + OpenVPN-generic (R8-Q37).

**Egress-mesh-routing** via per-app SOCKS5 over Nebula (R8-Q38): peer A's apps configured to route through peer B's VPN egress. Visualized as curved arc A→B→VPN cloud, dashed when active, flow particles animated (R8-Q39).

**Per-peer kill switch** (R8-Q40) with mesh-traffic exempted.

**DNS leak detection + one-click fix** (R8-Q41).

VPN tab in Control + status display everywhere appropriate.

---

## 9. Container management (Round 10, CONTAINER-*)

World-class container/pod management in Control. Promoted to **6th top-level Control category** (R10-Q16) alongside Hardware/Network/Customize/Maintenance/About.

### 9.1 Landing (R10-Q17)

Split layout: **top** = mesh-wide container summary across all peers; **bottom** = local containers.

Sub-categories: Containers / Pods / Images / Volumes / Networks / Registries / Compose-projects / Builds (8 sub-categories).

### 9.2 Container operations (R10-Q18)

Per-container right-click: Start / Stop / Restart / Pause / Unpause / Kill / Remove (with confirm) / Exec shell / View logs in Library / Copy CLI / Inspect JSON / Stats live-graph + **Move to peer** (live-migrate via podman save | podman load over Nebula) + **Promote to systemd service** (podman generate systemd).

**Live stats (R10-Q19):** inline sparklines on each running-container card (60s rolling, 1s refresh) for CPU/MEM/NET/DISK.

### 9.3 Images (R10-Q20)

Pull / Push / Build / Prune — full operations with progress as activity cards + live build logs.

**No vulnerability scanning in v6.0** (R10-Q23) — defer.

### 9.4 Compose (R10-Q21)

Compose-projects as cards. Each `.yml` in `~/.config/containers/compose/`. Supports podman-compose + Quadlet generation.

### 9.5 Registries (R10-Q22)

Registries as cards (login state + last-used + image-count). Built-in docker.io / quay.io / ghcr.io / gitlab + 'Add registry' modal.

### 9.6 Logs viewer (R10-Q24)

Library opens the container's log file `(~/.local/share/containers/storage/overlay/<id>/<id>-json.log)` with live-tail + filter + search.

### 9.7 Exec shell (R10-Q25)

Spawns terminal i3-tile running `podman exec -it <id>` next to Portal.

### 9.8 Cross-peer (R10-Q26)

Each remote peer's containers grouped in the mesh-wide summary; click drills; actions execute via SSH-RPC to remote peer's podman socket.

### 9.9 Pods (R10-Q27, R10-Q30 to R10-Q34)

**Pods as parent cards with container children** — composite cards (matches R5-Q10 children).

**Cross-peer pod networking (R10-Q30):** Nebula L3 overlay; pods get mesh-wide IP range `10.42.0.0/16`; service discovery via mesh DNS `<pod>.pods.mesh.mde` (R10-Q33).

**Scheduling (R10-Q31):** user picks per pod in launch modal; default = local peer.

**Network policies (R10-Q32):** allow-all default + explicit deny rules (mesh-trust).

**Restart policy (R10-Q34):** standard podman `--restart=` Never/On-failure(5)/Always.

### 9.10 Volumes + Networks (R10-Q28, R10-Q29)

**Volumes as cards** with size + containers-using-it chip + 'Browse in Library' bridge.

**Networks as cards** with connected-container children; visible on globe as subnet halos around hosting peer.

### 9.11 Security defaults (R10-Q35)

Privileged-allowed by default + per-container toggle. Compatibility-first.

---

## 10. Activity-as-files subsystem

8 activity types stored as JSON files (R2-Q6):

`~/.local/share/mde/activity/<type>/<iso8601>-<hash>.json`

Types: notifications, calls, kdc, clipboard, filechange, alerts, logins, media.

**Schema** (R2-Q3): FDO Notifications mirror + MDE extensions; `state: { read, starred, pinned, tags }` embedded (no snoozed_until — R2-Q32).

**Retention per type (R2-Q5):** notifications 7d, calls 90d, KDC 30d, filechange 14d, others 7d default. Star bypasses auto-delete. Pin sorts to top.

**Sync:** GlusterFS replication + local inotify; ≤ 3s e2e (R2-Q9).

**Emit:** FDO Notifications (legacy + apps) + new `dev.mackes.MDE.Activity.Emit(type, payload)` (R2-Q8).

**Clipboard Local-Only mode** — toggle prevents clipboard items syncing across mesh (R2-Q87).

**Cross-peer reaction:** all peers react to every activity (R2-Q37). Mesh-wide absolute DND (R2-Q38, softened by R4-Q39 grey segments).

---

## 11. Tech stack

| Surface | Tool |
|---|---|
| Language | Rust |
| UI framework | Iced + libcosmic (R2-Q62) |
| Window manager IPC | swayipc-async (R2-Q61) |
| Wayland surfaces | wlr-layer-shell + Iced smithay backend (R2-Q63) |
| D-Bus | dev.mackes.MDE.Portal (Goto/Focus/Lock/ToggleDND) + Activity.Emit + Dock (R2-Q64) |
| Tray | StatusNotifierItem + full XEmbed (R3-Q29) |
| Theming | MDE GTK4 + Qt6 themes + xdg-desktop-portal-mde (R2-Q66) |
| Logging | tracing → journald + live tail in About (R2-Q70) |
| Daemon | Persistent per-session, 50–80MB RSS, mded-supervised (Q98) |
| Container runtime | podman + podman-compose / Quadlet (R10) |
| XDR | Crowdsec (R7-Q5) |
| SIP stack | PJSIP (R8-Q86) multi-account: mesh + per-peer Vitelity sub-account direct (R11 lock 2026-05-26 retires Kamailio) |
| Group-call media | mediasoup ephemeral bridge spawned on best-uplink peer per call (R11) |
| Geo | MaxMind GeoLite2 offline (R8-Q77) |
| Flow collection | Pure Rust eBPF module (R8-Q6) |
| Echo cancel + denoise | RNNoise + WebRTC AEC3 (R9-Q14) |
| Font | **Intel One Mono everywhere** via xdg-desktop-portal-mde (system-wide) |
| Icons | **Carbon Icon Set first, Nerd Glyphs fallback**, both monochrome |

---

## 12. Visual identity

- **Color:** translucent overlays for Portal-full + solid charcoal Dock strip (off-white in light)
- **Pastel palette** for tags (curated 12-color, hashed assignment with user override)
- **Group-color accent** tints UI based on currently-drilled tag
- **Subtle 120ms hover fades** + no ripples (Q33)
- **Card transitions:** shared-element morph + spring overshoot (R5-Q8)
- **Cascade slide-in:** 200ms ease-out (Q21)
- **Selection:** filled background + 2px outline in group color (Q35)
- **Window chrome (i3 tiles):** 2px focused-window border in accent + 8px gaps + native i3 tab bar
- **Sound design:** typewriter tick on segment append + per-activity-type cues (notif ping / call ring / file tick / alert siren) + lock-engaged thud + unlock ping
- **15 default wallpapers** (5 curated + animated M + 10 community), or mesh-wallpaper
- **Easter eggs** (R4 carry): 7-click M-watermark = credits; `#!` tag name = CrunchBang ASCII tribute; Konami code = chiptune dev console

---

## 13. Lifecycle

### 13.1 Startup order (R2-Q57)

`sway → mded → mde-portal → user autostarts`. Bindings via swayipc-async at runtime (R2-Q69) — no sway config file pollution.

### 13.2 Boot theater (R2-Q100, R4-Q49)

Real systemd telemetry rendered around assembling M watermark. Breadcrumb types itself in left-to-right (60 chars/sec) in sync with service-start events. 2–3s budget.

### 13.3 First run (R2-Q58)

Birthright wizard (runs once): fonts, wallpaper, mesh enrolment, theme, DND hours. **Captive until mesh-home mounts** (R2-Q89).

### 13.4 Shutdown theater (R2-Q99, R4-Q50)

Reverse-typewriter disassembly: widgets fade → mini-tree disassembles → running-zone empties → pinned-zone empties → hostname backspaces → M backspaces → final character dim. Tied to systemd service-stop events.

### 13.5 Crash recovery (R2-Q59)

5s snapshot to `~/.cache/mde/shell-state.json`; full state restores on respawn — drilled paths + notification segments + cross-peer view (R4-Q48).

### 13.6 Multi-user (R2-Q60)

Single-user per machine; multi-user via standard Linux logout/login.

### 13.7 Lock screen (R2-Q54, R4-Q4)

Layer-shell overlay (replaces swaylock). Minimal `M › hostname` breadcrumb + clock + date + mesh pip + weather + battery + wifi/mesh icons. Latest 3 Recent previews read-only.

---

## 14. Worklist mapping

`docs/PROJECT_WORKLIST.md` § "v6.0 mde-portal" with 4 sub-sections:

- **Portal-*** — core shell (Dock breadcrumb, 6-button nav, Hub/Library/Control layers, cards subsystem, enrichment, scratchpad mechanics, lock screen, theaters, wallpaper)
- **VOIP-*** — PJSIP multi-account (mesh + Vitelity direct), Vitelity SimRing + focus filter, dialer UI, contacts, recording, SMS, video, ephemeral media bridge for group calls (R9-Q20 + R11 lock)
- **MESH-*** with 5 subgroups: A (assessment + Crowdsec), G (globe rendering), W (wireframe), V (visualizations), WP (wallpaper) — R8-Q99
- **CONTAINER-*** — Podman full management

Status legend + task schema per CLAUDE.md §1.

### 14.1 Build ordering (R10-Q50)

1. **Dock baseline** — layer-shell + 6 buttons + breadcrumb chevrons + clock + show-wp + Intel One Mono
2. **Hub layer + cascade + tag system**
3. **Library + activity files**
4. **Control + initial panels**
5. Parallel: VOIP / MESH / CONTAINER workstreams

Each commit ships END-TO-END (no stubs per `feedback_no_stubs`). Build fully authorized (R10-Q49).

---

## 15. Supersedes

This release retires / supersedes:

- `mackes/start_menu.rs` (the v1.1.0 popover)
- `crates/mde-files/` and `crates/mde-workbench/` (folded into mde-portal layers — hard cut per R1-Q108)
- v8.7 top-bar window-management buttons (newer-wins via Q113)
- Standalone notification surface (folded into Library + Notifications button popover)
- swaylock theming (Lock screen is a Portal surface)
- 'Recent' as a curated UI surface (R3-Q20 — activity files still write durably)
- Separate App-switcher Wayland overlay (R3-Q41 — switcher lives on the Dock)
- Q31 typography (Geologica + Plex Mono) — Intel One Mono everywhere
- M-badge bottom-left on Dock (replaced with 6 direct buttons; M lives only in wallpaper watermark — R10-Q6)
- **R3-Q44 i3 contract (Mod+r never intercepted)** — Round 12 R12-Q6 amends: Mod+r now repointed at sway-config level to launch Portal-compact resize widget. The rest of the contract (Mod+arrow / Mod+4–9 always reach sway; Mod+1/2/3 only when Portal focused) is unchanged.
- **R5-Q23 scratchpad badge in running-zone** — Round 12 R12-Q24 retires: sway's `scratchpad` is reserved for the Portal-full instance only; arbitrary windows are not promote-able. Running-zone never shows a scratchpad badge.
- **R4-Q67 'minimize-to-scratchpad' micro-button** — Round 12 R12-Q24 reframes: the 5th micro-button is plain 'minimize' (move-to-mark `parked` + jump-to-previous-workspace). Sway scratchpad untouched.
- **`smart_borders no` in `data/sway/config:44`** — Round 12 R12-Q20 flips to `smart_borders on` (matches `smart_gaps on`).

---

## 16. Round 12 — i3/sway integration tightening (2026-05-26)

**Lock context.** 25-Q AskUserQuestion survey 2026-05-26 in response to operator
directive *"Review i3 documentation and our design. I want to make the very best
use of i3."* Platform was feature-locked per CLAUDE.md §0.16; operator lifted the
lock explicitly for this survey scope, the lock re-engages on lift-completion.
**i3 here means sway** — the v2.0+ stack is Wayland-only per
[[project_v2_0_0_mackes_de]]; sway is i3's Wayland fork with the same config
syntax + the same IPC surface.

**Brand sub-statement (R12 directive):** *"Portal is the lens; sway is the
muscle. The shell exposes every i3/sway power feature as a Card or a breadcrumb
segment — never as a config file the operator has to learn."*

### 16.1 Workspace model (R12-Q1 .. R12-Q4)

- **R12-Q1 Workspace names auto-derive from focused window class** — mded
  subscribes to sway `window::focus` events; on each focus change renames the
  current workspace to the focused window's `app_id` (e.g., `firefox`,
  `helix`). The rename uses swayipc `rename workspace <num> to "<num>: <app_id>"`.
  Mini-tree cells (Portal-5) render this name per R4-Q66 + marquee on overflow
  per R4-Q64. **No manual rename UI** — the auto-derive is authoritative; the
  operator influences names by focusing the window they care about.
- **R12-Q2 Workspace output is tag-driven** — tag JSON (Portal-18 schema) gains
  a `preferred_output: <output-name>` field. When a workspace is created under a
  tag context (per Hub launch flow), sway `move workspace to output <preferred>`
  fires. Operator's tag taxonomy doubles as physical layout policy. Tag editor
  (Portal-18) gains an output-picker row.
- **R12-Q3 Workspace history surfaces as a transient breadcrumb 'previous'
  segment** — mded tracks a 5-deep LRU of visited workspaces. On every workspace
  focus change, the Dock renders a transient segment immediately LEFT of the
  mini-tree (after notifications, before mini-tree) labeled with the previous
  workspace's auto-derived name + tag color. Click = jump to previous workspace.
  Auto-dismiss 5s after the focus change that spawned it. No new keybind beyond
  click; preserves the i3 contract.
- **R12-Q4 Per-tag default layout** — Portal-18 tag schema gains
  `default_layout: 'splith' | 'splitv' | 'tabbed' | 'stacked'`. Right-click
  iconic Layout Chooser (R3 Q62 directive) on any tag-card in Hub sets the
  default. Launching the first window into a tag-owned workspace applies the
  tag default via swayipc `layout <name>`. Mesh-synced via GFS.

### 16.2 Modes (R12-Q5 .. R12-Q7)

- **R12-Q5 Mode segment far-left of breadcrumb** — when sway enters a non-default
  binding mode (`resize`, custom modes from R12-Q7), Portal renders a dedicated
  segment at the far LEFT of the breadcrumb (left of M; shares the
  notification-segment area per R4-Q34): `MODE: <name> — <hint text>` with
  R4-Q22 typewriter entry. Auto-removed on mode-exit. Uses the existing IPC
  `mode` event (no polling). Mode segment color = sway-internal modes (cyan)
  vs operator-defined modes (the mode's owning tag color).
- **R12-Q6 Mod+r repointed to Portal-compact** — `data/sway/config:137`
  (`bindsym $mod+r mode "resize"`) is replaced with
  `bindsym $mod+r exec mde-portal-compact resize`. Portal-compact (R6 surface)
  gains a `resize` panel: 4-direction visual handles + percent slider snapped
  to the focused tile's `percent` value (from `GET_TREE`); drag/slider edits
  emit swayipc `resize set <ppt> ppt` commands. The native sway `mode "resize"`
  block (lines 129–136) is REMOVED — operators wanting arrow-key resize fall
  through to Portal-compact's keyboard mode (h/j/k/l inside the panel mirrors
  the legacy bindings). **This amends the R3-Q44 i3 contract**; documented in
  §15 Supersedes.
- **R12-Q7 Hub-driven mode entry** — right-click any tag card in Hub gains an
  'Enter mode' action. Selecting it runs swayipc `mode <tag-name>` (mded
  pre-registered the sway-mode at startup by walking tag.json). Inside the
  mode, sway is bound: Mod+1..9 jumps to workspaces owned by that tag (mded
  reads tag membership and resolves workspace numbers at mode-entry time);
  Esc exits. Mode segment (R12-Q5) shows the tag color. **No mnemonic letter
  bindings** — Hub right-click is the canonical entry; power users may still
  add their own `bindsym ... mode "..."` lines via `~/.config/sway/config.d/`.

### 16.3 Marks (R12-Q8 .. R12-Q10)

- **R12-Q8 Auto-mark by app_id taxonomy** — new `crates/mackesd/src/workers/auto_mark.rs`
  worker subscribes to sway `window::new`. Per a static taxonomy table:
  `editors {helix, code, vim, emacs} → 'editor'`,
  `browsers {firefox, chromium, librewolf, qutebrowser} → 'web'`,
  `terminals {foot, alacritty, kitty, wezterm} → 'shell'`,
  `mail {thunderbird, geary, evolution} → 'mail'`,
  `chat {discord, element, signal-desktop, telegram-desktop} → 'chat'`.
  Unmatched app_ids get no mark. Operator-set marks via `mark` command are
  preserved (the worker checks `node.marks[]` first and skips if already
  marked). Marks are LOCAL (no peer prefix per R12-Q10).
- **R12-Q9 Mark pill below running-zone icon** — Portal-8.a card render gains
  a 8px pill positioned 2px below the icon. Color taxonomy:
  `editor=#42be65 (green)`, `web=#33b1ff (blue)`, `shell=#8d8d8d (charcoal)`,
  `mail=#ff8389 (orange)`, `chat=#be95ff (purple)`. Aero-peek tooltip
  (R3-Q11) gains a `Mark: editor` line. Unmarked windows show no pill.
- **R12-Q10 Lazy-fetch cross-peer marks** — local marks stay unprefixed.
  On cross-peer view activation (R4-Q6 hostname-cycle), Portal fires a
  `dev.mackes.MDE.AutoMark.GetMarks()` zbus call to that peer's mded over
  Nebula. Response is rendered for that view only; cached 30s. Falls back
  to empty list on unreachable. **NO mesh-home sync of marks** — they're
  ephemeral local state, recomputed at peer-boot.

### 16.4 Templates + restore (R12-Q11 .. R12-Q13)

- **R12-Q11 Prompt-on-change layout banner** — when the operator changes
  layout in a workspace owned by a tag (sway `binding` event on `layout *`),
  Portal renders a transient breadcrumb segment immediately right of the
  changed workspace's mini-tree cell: `Make <tag> default? ✓ ✕` in the tag's
  color. Click ✓ → rewrites `tag.default_layout` (R12-Q4) + mesh-syncs.
  Click ✕ → writes a per-workspace override to `~/.local/share/mde/workspaces.json`.
  Auto-dismiss 8s. Repeated layout flips within the dismiss window collapse
  into one prompt (last layout wins).
- **R12-Q12 Templates as Card type** — new card kind `template` (extends
  Portal-31's `CardKind` enum). Storage:
  `~/.local/share/mde/templates/<ulid>.json` containing
  `{name, output_count, sway_tree: <swaymsg -t get_tree filtered>, ws_layout: <name>, swallows: [<criteria>]}`.
  Hub gains a system tag `Templates` (sibling of `All apps` / `Untagged` /
  `Workspaces` / `Settings` / `Power` / `Mesh`). Right-click any mini-tree
  workspace cell adds 'Save as template…' → operator names the template;
  template card flies into the Templates tag-card column (R5 cascade
  morph). Click a template card → swayipc creates a fresh workspace +
  `append_layout <path>` populates the slots; placeholders hold for
  swallow-criteria matches until the app launches. Templates mesh-sync via
  GFS so any peer can apply any saved template. **Templates inherit
  R5-Q3's 12-field schema** but with `type='template'` + a `template_data`
  blob in the `body_text` field.
- **R12-Q13 Restore workspaces + layouts (not apps)** — on mded startup
  post-login, the session-persistence worker reads
  `~/.local/share/mde/session.json` (written 5s-snapshotted per R2-Q59).
  Restores: workspace name+output+layout per workspace. Apps do NOT
  auto-launch. Sway `swallows` criteria (matched by `app_id`) hold open
  the placeholders for 30 minutes; when the operator launches an app that
  matches a placeholder, sway slots it in. Placeholders that don't fill in
  30 minutes collapse to default-layout. Fast first-paint preserves the
  desktop shape without locking the operator into reopening everything.

### 16.5 Rules + autostart (R12-Q14 .. R12-Q16)

- **R12-Q14 Window-rules: both per-app + Control overview** — single store at
  `~/.config/mde/window-rules.toml`. Per-rule shape:
  `[[rule]]\n match = "Firefox"\n float = true|false\n sticky = bool\n
  fullscreen_on_start = bool\n border_width = <px>\n mark = "<string>"\n
  assign_workspace = <num>\n focus_policy = "smart|urgent|focus|none"`.
  mded translates rules into runtime swayipc `for_window` registrations on
  every sway start (via `swaymsg --type subscribe '["barconfig_update"]'`
  for sway reload events too). Hub right-click any app card → 'Window
  rules…' modal pops a form bound to that app's match key. Control gains a
  'Window Rules' panel under Customize that lists all rules + add/edit/delete.
  **No sway config file editing** — the TOML is authoritative; sway config
  carries only the BUG-15-replacement default rules.
- **R12-Q15 Per-tag launch assigns workspace** — when Hub launches an app
  under a drilled tag context, mded `move container to workspace <tag-ws>`
  on the window-new event. Tag IS the assign rule; no separate per-app
  `assign_workspace` step needed unless R12-Q14 explicitly overrides.
- **R12-Q16 Per-tag autostart** — Portal-18 tag schema gains
  `autostart: [<app-id>]` array. When the tag's first workspace is created
  (e.g., on Hub drill into the tag's first launch), mded launches each
  listed app via `swaymsg exec <cmd>` (NOT sway config `exec` lines). Apps
  inherit the tag context (Q15 assigns them to the tag's workspace). On
  session restore (R12-Q13), tag-autostart fires for tags whose workspaces
  are being restored. XDG autostart spec is NOT honored beyond what mded
  inherits from the systemd user session; tag-driven is the only mechanism.

### 16.6 Focus + cursor policy (R12-Q17 .. R12-Q19)

- **R12-Q17 `mouse_warping container`** — cursor jumps to focused container's
  center on focus change. Pairs with keyboard-driven workflow. Written into
  `data/sway/config` directly: `mouse_warping container`.
- **R12-Q18 `focus_follows_mouse no`** — already in `data/sway/config:73`.
  Unchanged.
- **R12-Q19 `focus_on_window_activation urgent`** — written into
  `data/sway/config`: `focus_on_window_activation urgent`. Background apps
  NEVER steal focus; they set the urgent flag instead. Per-app override via
  R12-Q14 window-rules `focus_policy` field.

### 16.7 Visual (R12-Q20 .. R12-Q22)

- **R12-Q20 Smart gaps + smart borders both on** —
  `data/sway/config:44` flips from `smart_borders no` to `smart_borders on`.
  `smart_gaps on` (line 43) unchanged. Single-window workspaces become
  full-bleed; multi-window workspaces show 4px gaps + 4px focused border
  (per current config + BUG-10 thickening).
- **R12-Q21 Focused border = owning-tag color** — mded `border_color`
  worker subscribes to sway `workspace::focus` events. For each workspace,
  looks up its owning tag (from R12-Q4 + Portal-18 tag membership), reads
  the tag's group-color, and emits swayipc `client.focused
  <color> <color> #f4f4f4 <color> <color>` (5-arg form: border, bg, text,
  indicator, child_border). Border updates apply immediately. Unfocused +
  urgent + placeholder colors stay platform-default (lines 48–51 of
  `data/sway/config`). Tagless workspaces fall back to the existing
  `#2b9af3` Carbon blue.
- **R12-Q22 Urgent-window cascade — all three channels** — when sway emits
  `window::urgent`, mded fans out: (1) M-badge crit-pulse via Bus
  `bus/mbadge/pulse` topic (R3-Q4 vocabulary); (2) mini-tree cell pulses
  in the workspace's owning-tag color via Portal-5 IPC subscription; (3)
  breadcrumb left-of-M notification segment with typewriter entry of the
  app's display name (R4-Q35). Click any of the three jumps to the urgent
  window via swayipc `[con_id=<n>] focus`. Three-channel redundancy matches
  Q19's no-focus-steal policy — operator must notice + act.

### 16.8 Drag-to-tile + scratchpad retirement (R12-Q23 .. R12-Q24)

- **R12-Q23 Hub card drag → launch into target slot** — picking up a Hub
  app/file card and dragging over an open workspace triggers Portal to
  render drop indicators on each tile boundary (top/right/bottom/left
  split + tabbed-merge + stacked-merge + center-replace). Indicators are
  16px translucent overlays in the drilled-tag color. Release on an
  indicator: Portal fires `swaymsg [con_id=<focused>] focus` then `exec
  <app-cmd>` then waits for the new window's `window::new` event and
  emits the appropriate `split|move|layout` swayipc command. Cancel by
  Esc or by releasing outside the workspace. File cards launch their
  XDG default opener. **NOT extended to drag-from-running-zone** — moving
  existing windows uses sway-native titlebar drag (preserved).
- **R12-Q24 Scratchpad reserved for Portal-full only** — sway's scratchpad
  becomes a Portal-internal mechanism. **Retires:**
  - **R5-Q23 scratchpad badge in running-zone** — Portal-8.a never shows
    a scratchpad badge; running-zone shows only top-level (non-scratchpad)
    windows.
  - **R4-Q67 5th micro-button 'minimize-to-scratchpad'** — renames to
    plain 'minimize'. Implementation: mded `move container to mark "_parked"`
    + `workspace number 99 "parked"` for any minimized window; the
    micro-button is bound to that command. Workspace 99 is the parking
    zone; it lives across all outputs and isn't shown in mini-tree (mded
    filter). The existing `data/sway/config:121` BUG-15 binding
    (`$mod+Shift+m → swaymsg scratchpad show`) is rewritten to
    `$mod+Shift+m → swaymsg [workspace=99] move container to workspace current`
    (un-park the most-recent parked window).

### 16.9 IPC (R12-Q25)

- **R12-Q25 Bus everywhere — no tick use** — affirms the 100-Q Q20 lock
  ([[feedback_platform_feature_locked]]). Mackes Bus
  ([[project_v6_x_mackes_bus]] BUS-1..BUS-7) is the canonical MDE-internal
  IPC for cross-process events. Sway IPC `tick` is NOT used; mded
  subscribes to sway events (workspace, window, mode, output, binding)
  and republishes interesting state changes as Bus messages on
  `bus/sway/<event>` topics. Subscribers (Portal, applets) read Bus, not
  sway IPC directly — preserves one event-bus model across the platform.

### 16.10 Build ordering

After Round 12 lifts into the worklist as **Portal-41 .. Portal-59** under
the existing v6.0 mde-portal section. Dependencies:

1. **Portal-41** (auto-workspace-name) + **Portal-48** (auto-mark) can
   land in parallel — both are independent mded workers.
2. **Portal-53** (window-rules subsystem) blocks **Portal-49** (mark pill
   render reads taxonomy from rules), **Portal-56** (border-color worker
   reads tag-from-workspace), and **Portal-58** (drag-to-tile reads
   focus_policy).
3. **Portal-51** (templates) blocks **Portal-52** (session restore — both
   use sway swallows mechanism).
4. **Portal-55** (sway config alignment, Q17+Q19+Q20) lands FIRST as a
   single small commit — unblocks everything else by making the config
   match the new defaults.
5. **Portal-59** (scratchpad retirement) coordinates with the running-zone
   render code (Portal-8.a) and the 5th micro-button (R4-Q67) — single
   commit covering all three sites.

### 16.11 What did NOT change

Locks from Rounds 1–11 that an operator might expect Round 12 to touch
but didn't:

- **i3 contract** (R3-Q44) — Mod+arrow / Mod+4–9 / Mod+1/2/3-when-not-Portal-focused
  still reach sway untouched. Only Mod+r was amended (R12-Q6).
- **Universal Card schema** (R5-Q3) — 12 fields unchanged; templates
  (R12-Q12) use the existing schema with `type='template'`.
- **Customize knob list** (R4-Q85) — still 5 knobs (palette, typewriter-speed,
  chevron-style, font-weight, sound-on-off). Round 12 sets defaults for
  Q17 / Q19 / Q20 / Q21 but doesn't add operator-tunable knobs for them.
- **Bus as canonical IPC** (Q20 of 100-Q) — affirmed by R12-Q25.
- **Tag system architecture** (Portal-18) — Round 12 EXTENDS the schema
  with `preferred_output` + `default_layout` + `autostart[]` fields; the
  core tag mental model (manual / smart / preset, 3-level nesting cap)
  is unchanged.

---

*Locked: 2026-05-24/25 across 10 question rounds + R11 VoIP simplification 2026-05-26 + R12 i3/sway integration tightening 2026-05-26 (~598 decisions total).*
*Source-of-truth memory: `~/.claude/projects/-home-mm-Desktop-files-mackes-shell/memory/project_v6_0_mde_portal.md`.*
