# MDE-X — default i3 config (XOrg edition of data/sway/config)
# ============================================================
#
# v2.0.0-xorg — port of data/sway/config for the i3/XOrg line.
# mde-session-xorg seeds ~/.config/i3/ from this directory on
# first login (mirrors the sway config's mde-session-seeds behaviour).
# Keybindings mirror the sway config so muscle memory carries across
# the Wayland ↔ XOrg switch.
#
# Reset any time via Workbench → System → Window Manager →
# "Reset i3 config", or by copying
# /usr/share/mde-xorg/i3/mde-xorg.config back to ~/.config/i3/config.

# ---------------------------------------------------------------------------
# Modifier — Super (the "Windows" key)
# ---------------------------------------------------------------------------
set $mod Mod4

font pango:Red Hat Text 11

# ---------------------------------------------------------------------------
# Look & feel — borders, gaps, colors (same Carbon palette as sway config)
# ---------------------------------------------------------------------------
default_border pixel 1
default_floating_border pixel 1
gaps inner 4
gaps outer 0
smart_gaps on
smart_borders on

#                       border       bg       text      indicator child_border
client.focused          #2b9af3      #2b9af3  #f4f4f4   #4589ff   #2b9af3
client.focused_inactive #393939      #393939  #f4f4f4   #393939   #393939
client.unfocused        #262626      #262626  #a8a8a8   #262626   #262626
client.urgent           #fa4d56      #fa4d56  #f4f4f4   #fa4d56   #fa4d56
client.placeholder      #1a1a1a      #1a1a1a  #a8a8a8   #1a1a1a   #1a1a1a

# ---------------------------------------------------------------------------
# Workspaces (4, persistent)
# ---------------------------------------------------------------------------
set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"

bindsym $mod+1 workspace $ws1
bindsym $mod+2 workspace $ws2
bindsym $mod+3 workspace $ws3
bindsym $mod+4 workspace $ws4
bindsym $mod+Shift+1 move container to workspace $ws1
bindsym $mod+Shift+2 move container to workspace $ws2
bindsym $mod+Shift+3 move container to workspace $ws3
bindsym $mod+Shift+4 move container to workspace $ws4

# ---------------------------------------------------------------------------
# Focus / move
# ---------------------------------------------------------------------------
focus_follows_mouse no

bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right

# ---------------------------------------------------------------------------
# Layout switching
# ---------------------------------------------------------------------------
bindsym $mod+e layout toggle split
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+f fullscreen toggle
bindsym $mod+space floating toggle
bindsym $mod+Shift+space focus mode_toggle

# ---------------------------------------------------------------------------
# Window operations
# ---------------------------------------------------------------------------
bindsym $mod+q kill
bindsym $mod+Return exec xfce4-terminal
bindsym $mod+d exec dmenu_run -fn "Red Hat Text-11" -nb "#262626" -nf "#f4f4f4" -sb "#2b9af3" -sf "#f4f4f4"

mode "resize" {
    bindsym h resize shrink width  10 px or 10 ppt
    bindsym j resize grow   height 10 px or 10 ppt
    bindsym k resize shrink height 10 px or 10 ppt
    bindsym l resize grow   width  10 px or 10 ppt
    bindsym Return mode "default"
    bindsym Escape mode "default"
}
bindsym $mod+r mode "resize"

# i3 itself
bindsym $mod+Shift+c reload
bindsym $mod+Shift+r restart

# ---------------------------------------------------------------------------
# Reserved — owned by mde-panel / mde-session
# ---------------------------------------------------------------------------
#  Super+M               Notification drawer (mde --drawer)
#  XF86Audio*            Volume (pactl)
#  XF86MonBrightness*    Brightness (brightnessctl)

# ---------------------------------------------------------------------------
# Window rules — float MDE panel surfaces + dialogs
# ---------------------------------------------------------------------------
# i3 uses WM_CLASS (class= criterion); mde-panel sets WM_CLASS to
# "mde-panel" and mde-workbench to "mde-workbench".
for_window [class="^mde-panel$"]      floating enable, border none
for_window [class="^mde-workbench$"]  floating enable
for_window [class="^mde-session$"]    floating enable
for_window [window_role="pop-up"]     floating enable
for_window [window_role="task_dialog"] floating enable
for_window [window_type="dialog"]     floating enable

# ---------------------------------------------------------------------------
# No i3bar — mde-panel replaces it
# ---------------------------------------------------------------------------
# (intentionally no `bar { ... }` block)

# ---------------------------------------------------------------------------
# MDE-managed includes
# ---------------------------------------------------------------------------
# settings::keybinds writes mackes-bindings.conf into this directory;
# mde-xorg-defaults.conf lands here from the RPM install.
include ~/.config/i3/config.d/*.conf
